[package]
name = "ids_std_rest_api"
version = "0.1.0"
edition = "2021"

[dependencies]
validator = { workspace = true }
axum = "0.7"
serde = { version = "1.0", features = ["derive"] }
ids_std_domain = { path = "../ids_std_domain" }
